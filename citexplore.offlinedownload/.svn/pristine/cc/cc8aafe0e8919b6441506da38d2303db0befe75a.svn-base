package citexplore.offlinedownload.manager;

import com.sun.istack.internal.NotNull;

/**
 * 离线下载器节点池监听者。
 *
 * @author Zhang, Yin
 */
public interface PoolListener {

    // **************** 公开变量

    // **************** 私有变量

    // **************** 继承方法

    // **************** 公开方法

    /**
     * 离线下载器节点已死亡。
     *
     * @param node 死亡的离线下载器节点。
     */
    void nodeDied(@NotNull DownloaderNode node);

    /**
     * 离线下载器节点已恢复。
     *
     * @param node 已恢复的离线下载器节点。
     */
    void nodeRecovered(@NotNull DownloaderNode node);

    // **************** 私有方法

}
